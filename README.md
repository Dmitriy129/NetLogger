# NetLogger

## SetUp

```bash
npm i
npm run build
```

## Start

```bash
npm start
```

app will be launched on `port: 3000` (default)

to change the `port` create a file .env.local

``` env
PORT="8080"
```
